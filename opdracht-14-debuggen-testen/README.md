# Opdracht 14 – Debuggen & Testen

**Folder:** `opdracht-14-debuggen-testen`

**Setup:**
```bash
cd opdracht-14-debuggen-testen
npm install
npm run dev
```

---

Debug een bestaande React-applicatie die fouten bevat. Volg de volledige uitleg in `debugging_assignment.md` in de projectfolder.

**Oplevering:** Alle bugs opgelost (controleer de console) + de acceptatiecriteria-lijst doorlopen. Commit & Push + link inleveren via Canvas
